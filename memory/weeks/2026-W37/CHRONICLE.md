# Chronicle — Week 2026-W37 (Mon 07 – Sun 13)

## 2026-09-12

### [milestone] Инициализация памяти проекта
Создан `memory/` для `vesqor-chat-dev` перед началом работы над тикетом #VQ-25 (админ-страница сравнения ответов ChatGPT/Gemini/VESQOR). PROFILE/CONTEXT/FACTS/DECISIONS заполнены на основе SPEC.md, PLAN-RU.md и осмотра репозитория (git log, remotes, branches) — без отдельного интервью, т.к. проект и задача уже полностью документированы владельцем.
Ref: DECISIONS.md#001–#005

### [discovery] Стадия 0 — базовая линия гейтов и синхронизация
`vesqor` был на 3 коммита позади origin (не 6, как в плане) — фича seal/confidentiality toggle; подтянута fast-forward без конфликтов вместе с feature-веткой. Реальная alembic-голова — `v0e1s1q1r` (PLAN-RU.md ошибочно называл `v2p1r0e0s0e0t`). PR-гейт для базы `vesqor` — только `vesqor-build.yml` (signup-gate script + vite build); backend.yaml/frontend.yaml на PR в `vesqor` не срабатывают. Базовая линия зафиксирована как красная и не наша: `ruff format --check` (15 файлов), `svelte-check` (8407 ошибок/210 варнингов/356 файлов), `eslint .` (падает целиком на баге плагина в FilePreview.svelte) — не чиним. Зелёные: `ruff check --select=F`, оба `test/test_vesqor_*.py`, `vitest --run`, `vite build`.
Ref: FACTS.md#Gate Baseline

### [decision] Владелец: история вердиктов хранится целиком
Отчёты и сводки переведены на append-only с колонкой `revision`, как у ответов. Пересуд и перегенерация ничего не затирают; устаревшее по-прежнему выводится, а не хранится флагом. Решение пришло, когда агент уже писал код по прежней схеме — перенаправлен на лету без потери работы. Тогда же одобрено добавление прогона тестов VQ-25 в `vesqor-build.yml` на стадии 7.
Ref: DECISIONS.md#006, DECISIONS.md#007

### [issue] Ревью стадии 1 нашло утечку секрета в ответе API
`_sanitize_base_url` срезал query и user-info только у URL вида `scheme://netloc`; значение `user:TOKEN@host/v1` пробрасывалось как есть в тело `GET /api/v1/compare/config`, а три варианта опечатки в переменной окружения роняли `ValueError` → HTTP 500. Первопричина — в спецификации, которая не определяла поведение для непарсящегося URL. Правило переписано на fail-closed, ремонт выполнен и перепроверен.
Ref: DECISIONS.md#008

### [milestone] Стадия 1 VQ-25 закрыта: PASS по трём осям
Миграция `v2q5c0m0p0r` (единственная голова), четыре таблицы, модели с async-CRUD, env-резолвер провайдеров и admin-only `GET /api/v1/compare/config`. 24 теста, оба существующих гейт-скрипта и `ruff` зелёные; боевая локальная БД не тронута — миграция проверялась только на копии. Повторное ревью подтвердило append-only мутационной проверкой всех трёх таблиц и идентичность DDL моделей и миграции.
Ref: memory/weeks/2026-W37/2026-09-12/work-report-vq25-stage-1.md

### [discovery] Движок VESQOR оказался OpenAI-совместимой дверью
Разведка стадии 2 показала, что движок принимает `POST /api/v1/chat/completions` с агентским токеном `bs_live_…` и router-моделями, а чат ходит в него через `OPENAI_API_BASE_URL`/`OPENAI_API_KEY`. Отсюда две ошибки стадии 1 в работе с секретами — обе исправлены до того, как что-либо ушло наружу. Статус знания: гипотеза, проверить у владельца на стадии 7.
Ref: DECISIONS.md#009

### [issue] Дефект стадии 1: неудачный ретрай обесценил бы всё судейство
Чекпоинт стадии 2 нашёл, что «текущий ответ» вычислялся как максимальная ревизия без фильтра по статусу. После записи отказов строками неудачная попытка стала бы текущим ответом и сдвинула бы версии так, что все отчёты судей разом пометились бы устаревшими. Всплыло бы только на стадии 5.
Ref: DECISIONS.md#010

### [insight] Тест охранял функцию, которую страница не вызывала
Ревью 2b: логика состояний вынесена в модуль и покрыта тестами, но компонент писал тот же переход инлайном. Мутант «очистить ответ при старте перегенерации» прошёл все 21 тест. Закрыто структурной проверкой — теперь тест требует, чтобы каждое присваивание состояния шло через модуль.
Ref: INSIGHTS.md/What Doesn't Work

### [milestone] Стадия 2 VQ-25 закрыта: три поставки, три PASS
Генерация работает end-to-end: клиент провайдеров, три эндпоинта, страница `/admin/compare` с тремя независимыми карточками. 85 pytest + 24 vitest, `svelte-check` без регресса базовой линии, сборка проходит.
Ref: memory/weeks/2026-W37/2026-09-12/work-report-vq25-stage-2.md

### [discovery] Превью-сервер владельца накатил миграцию на локальную базу
В 18:39 через `run-local.sh` поднят uvicorn на :8080; `run_migrations()` при старте применил `v2q5c0m0p0r` к `backend/data/webui.db`, там появился живой запуск. Обнаружено в момент, когда я собирался править эту миграцию на месте — вместо этого колонка `params` добавлена новой ревизией `v2q5p0a0r0m`. Инвариант «локальная база не тронута» заменён на «база на голове, не трогать, копировать три файла (WAL)».
Ref: DECISIONS.md#011

### [issue] Три дыры в спеке судьи, пойманные чекпоинтом до кода
Схема отчёта передавалась только в `response_format` первой ступени — на ступенях 2–3 модель выдумывала бы форму; тест маппинга был тавтологичен (пересчёт по тому же seed); лимит входа судьи брался из лимита генерации при входе в 3–4 раза больше. Все три всплыли бы на стадии 7.
Ref: STAGE-3-SPEC.md

### [decision] Маппинг меток на провайдеров — только на сервере
Тэлли стадии 4 считается из `map_report`; вторая реализация в TS дала бы «страница показывает одного победителя, тэлли считает другого». Сериализация отдаёт `mapped` рядом с сырым анонимным `report`; клиент только подставляет «(was B)».
Ref: STAGE-3B-SPEC.md Deliverable 0

### [milestone] Стадия 3 VQ-25 закрыта: судейство работает end-to-end
Промпт судьи побайтово по спеке, лестница структурного вывода с диагностикой в `params`, append-only отчёты с вычисляемым `outdated`, карточки отчётов на странице. 138 pytest + 44 vitest, `svelte-check` ровно на базовой линии, сборка проходит. Ревью 3a и 3b — PASS, 23 из 26 мутантов убиты сразу, три выживших закрыты.
Ref: memory/weeks/2026-W37/2026-09-12/work-report-vq25-stage-3.md

### [decision] Владелец: устаревшие вердикты в тэлли не считаются
Вариант А — итог только по свежим вердиктам (по текущим версиям ответов). Исключение видимо: каждый исключённый судья перечислен с причиной.
Ref: DECISIONS.md#012

### [milestone] Стадия 4 VQ-25 закрыта: тэлли в коде, run-all, панель
Строгое большинство свежих валидных вердиктов; ничья и «нет надёжного победителя» — в знаменатель; self-vote аннотирует, не меняет (мутант «удалить флаги» не сдвинул ни одного числа); `included_reports` заложен для стадии 5. Run-all через общую `judge_once`, три разных `label_map`. Панель не имеет доступа к отчётам и не считает. 171 pytest + 53 vitest; ревью 4a — 20/20 мутантов.
Ref: memory/weeks/2026-W37/2026-09-12/work-report-vq25-stage-4.md

## 2026-09-13

### [issue] Агент записал в превью-базу владельца, обойдя собственную защиту
При генерации золотого фикстура стадии 5 разовая команда `python3 -c`, импортирующая тестовый модуль **вне pytest**, потянула `open_webui.config` и применила `alembic upgrade head` к `backend/data/webui.db`. Корневой `conftest.py` защищает только внутри pytest. Применилась ровно аддитивная `v2q5p0a0r0m` (nullable-колонка), проверено архитектором read-only: `integrity ok`, данные целы (1 запуск, 3 ответа, 1 пользователь). Откат не делался — это была бы вторая несанкционированная запись, и она сломала бы превью при старте с текущим кодом. Отдельно и **без установленной причины**: сервер на :8080 перестал слушаться, процессов нет; совпадение по времени зафиксировано как наблюдение, не как вывод.
Ref: SCRATCH.md § INCIDENT 2026-09-13, INSIGHTS.md/What Doesn't Work

### [decision] Владелец: порог большинства и предупреждение о стоимости
Предпочтённый ответ требует минимум двух включённых судей (один проголосовавший большинства не даёт). Перед «Сгенерировать все три» и «Запустить всех судей» — диалог подтверждения с **динамическим** числом платных запросов; одиночные кнопки без трения; русская формулировка добавлена в `ru-RU`.
Ref: DECISIONS.md#014, #015

### [discovery] В превью колонки настроены на сторонние модели
Чтение превью-базы: ChatGPT→`glm-5.3`, Gemini→`kimi-k2.7-code`, VESQOR→`deepseek-v4-flash:0731`; все шесть попыток упали с HTTP 429. Страница отработала честно (по-карточные отказы, ничего не выдумано, соседние строки целы). Вопрос владельцу задан: временная проверка проводки или ошибка конфигурации — от этого зависит, сравнивает ли бенчмарк те системы, ради которых существует.

### [milestone] Стадия 5 VQ-25 закрыта: сводка собирается кодом
Десять секций, дословные цитаты судей, §1–§3 из тэлли, детерминированность под кросс-процессным сторожем, устаревание по двум измерениям, append-only ревизии. 201 pytest + 62 vitest. Главный урок: сторож детерминированности был инертен против того, ради чего написан — два сида и бедная фикстура; чинится усилением фикстуры, и только после падения мутанта фиксируется новый эталон.
Ref: memory/weeks/2026-W37/2026-09-13/work-report-vq25-stage-5.md

### [insight] Зелёный прогон оказался везением — второй раз за проект
Исполнитель сдал стадию 6a с 231 зелёным; на той же неизменённой базе у архитектора один тест упал. Тест брал `runs[0]`, полагая «последний созданный = первый», а при секундной точности порядок внутри секунды решает uuid4. Тот же класс, что флак маппинга в 3a (проходил 5 раз из 6). Вывод, записанный в практику: гонять сюиту многократно и не принимать единичный зелёный.
Ref: INSIGHTS.md/What Doesn't Work

### [insight] Структурная проверка по чёрному списку имён обходится
Ревью провело «Run again» мимо диалога стоимости через хелпер, объявленный выше по файлу: все 15 проверок прошли, платные запросы ушли. Соседняя проверка, сформулированная позитивно («в теле обязан быть вызов X и ничего кроме разрешённого»), оба обхода поймала. Правило: требовать нужного, а не запрещать известное плохое.

### [milestone] Стадия 6 VQ-25 закрыта — вся функциональность тикета построена
История запусков, родословная, переоткрытие, перезапуск без автогенерации, межзапусковая изоляция и матрица приёмки (16 требований константой в репозитории, по тесту на каждое, сторож от дрейфа). Ревью 6a — PASS без находок. 233 pytest + 74 vitest, `svelte-check` ровно на базовой линии, сборка проходит. Впереди только приёмка.
Ref: memory/weeks/2026-W37/2026-09-13/work-report-vq25-stage-6.md
