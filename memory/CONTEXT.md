# Current Context (updated: 2026-09-12)

## State
- **Working on**: Тикет #VQ-25. **Стадии 0–6 завершены — вся функциональность построена.** Ждём подтверждения владельца на Стадию 7 (гейты, живой E2E, визуальный проход, превью, затем коммит/пуш/PR по его команде).
- **Last completed**: Стадия 6 — список сохранённых запусков (выдержка промпта, три счётчика, курсор по паре, постоянное число обращений к БД), родословная перезапусков, история на странице, межзапусковая изоляция и **матрица приёмки: 16 требований тикета константой в репозитории, по тесту на каждое**. Ревью 6a — PASS без находок, 6b — PASS. **233 pytest + 74 vitest**. Работа НЕ закоммичена. Отчёт: `memory/weeks/2026-W37/2026-09-13/work-report-vq25-stage-6.md`.
- **Blocked by**: Живых ключей для трёх реальных систем нет; в превью настроены сторонние модели (см. ниже) — живой E2E на стадии 7.

## Active Tasks
- Следующая: **Стадия 7 — приёмка.** Полный прогон гейтов против базовых линий стадии 0; **список из 11 ручных проверок на живом превью** (`SCRATCH.md § STAGE 7 — manual checklist`) — то, что принципиально не покрывается 233+74 тестами, потому что всё проверено двойниками; сверка скопированного списка приёмки с тикетом руками; визуальный проход; превью владельцу; и только после его «пушь» — коммит, ветка, PR с базой `vesqor`.
- **Перед живым прогоном обязательно**: перенастроить колонки с временных моделей (`glm-5.3`, `kimi-k2.7-code`, `deepseek-v4-flash:0731`) на настоящие три системы.
- Вопросы владельцу на стадии 7: прогон VQ-25-тестов в CI (`#007`, одобрено — надо внести в `vesqor-build.yml`); `VESQOR_SERVICE_TOKEN`/`VESQOR_API_BASE_URL` не задокументированы в `.env.example`; отсутствие удаления/срока хранения истории; «requires a new review» реализовано как подсказка, а не запрет (единственное место, где ТЗ строже построенного); типизация `getContext` в чужом `admin/+layout.svelte` (сняла бы 12 предсуществующих ошибок).

## Обстановка
- **Превью-сервер владельца на :8080 работает, он им пользуется** — не трогать ни сервер, ни `webui.db`; счётчики строк меняются не нами.
- **Инцидент 2026-09-13**: агент записал в превью-базу через `python3 -c` вне pytest. Правило: любой `python3` с импортом `open_webui` — только с `ENABLE_DB_MIGRATIONS=False` и одноразовым `DATABASE_URL`. Подробности: `SCRATCH.md § INCIDENT`.
- **Модели в превью временные — владелец подтвердил 2026-09-13**: ChatGPT→`glm-5.3`, Gemini→`kimi-k2.7-code`, VESQOR→`deepseek-v4-flash:0731` поставлены для проверки проводки, менять не надо. Все попытки падали с HTTP 429 (rate limit шлюза) — это тоже ожидаемо. **На стадии 7 перед живым E2E колонки нужно перенастроить на настоящие три системы**, иначе бенчмарк сравнит чужие модели под их именами.

## Обстановка
- **Превью-сервер владельца на :8080** (`run-local.sh`), локальная `webui.db` на голове с живыми данными — не трогать ни базу, ни сервер; для судейства на превью нужен перезапуск (новая колонка `params`). База в WAL-режиме — копировать три файла.

## Вопросы, ждущие владельца (к стадии 7)
- Что реально лежит в `OPENAI_API_KEY` на Fly и какой адрес двери движка живой (на этом держится `DECISIONS.md#009`).
- Какой id модели ставить в колонку VESQOR (`vesqor-reasoning` / `vesqor-default`).
- Лимит таймаута прокси Fly (серверный таймаут 180с должен быть ниже). Смягчено: обрыв теперь ведёт к перечитыванию сохранённого состояния, а не к потере ответа.
- Известный пробел: один клик = три платных обращения к моделям, без счётчика стоимости.

## Recent Decisions
См. DECISIONS.md#001–#005 (перенесены из PLAN-RU.md §3)

## Watch Out
- Ветка `feat/vq25-answer-benchmark-2026-09-12` уже существует локально с незакоммиченными изменениями (vite.config.ts modified, много untracked static-файлов, run-local.sh, vite.config.ts.bak) — разобраться перед стадией 0, не затирать.
- Origin репозитория — `VESQOR-MEGA-AI/open-webui`, а не `Tigr23239/open-webui`, упомянутый в PLAN-RU.md §Git-флоу — уточнить у владельца, куда именно пушить, когда дойдём до пуша.
- Без «пушь» от Сергея — не коммитить и не пушить.
